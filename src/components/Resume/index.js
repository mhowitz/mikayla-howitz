import React from "react";

function Resume() {
    return(
        <section className="section is-large">
            <h2 id="resume" className="is-size-3">Resume</h2>
        </section>
    )
};

export default Resume;